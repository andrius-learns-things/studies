version: "3"
services:
  web:
    build:
      context: ./web
    ports:
      - "8059:1717"
    volumes:
      - ./web:/code
  tester:
    build:
      context: ./tester
    depends_on:
      - web
    volumes:
      - ./tester/code:/code
