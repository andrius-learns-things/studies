version: "3"
services:
  web:
    build:
      context: ./web
    volumes:
      - ./web:/code
  tester:
    build:
      context: ./tester
    depends_on:
      - web
    volumes:
      - ./tester/code:/code
