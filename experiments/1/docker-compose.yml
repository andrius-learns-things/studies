version: '3'
services:
  web:
    build:
      context: ./app
    ports:
      - "8051:5000"
    depends_on:
      - redis
      - mongo
  redis:
    image: redis
    ports:
      - "6379"
  mongo:
    image: mongo